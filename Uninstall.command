#!/bin/zsh
set -e
rm -rf "$HOME/Applications/IMGLESS.app"
printf '\nIMGLESS.app removed.\n'
printf 'Extensions and statistics remain in ~/Library/Application Support/IMGLESS and macOS preferences.\n'
printf 'Remove those manually only if you want a full reset.\n\n'
